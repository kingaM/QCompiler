fdef sum(i: int, j:int) : int {
     return i + j;
}

fdef sum2(i:float, j:float) : float {
     return i + j;
}

a : int = 0;


{

  s1 : int = sum(-10,20);
  s2 : float = sum(10.0,-20.0);

  if (s1 < s2 || s1 == s2) {
     b : bool =  s1 + s2 / (s1 + s2) >= 30;
  } else {
     0 + 1 - 1.2;
  }
}